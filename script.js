const gameBoard = gameBoardFactory([[null,null,null], 
                                    [null,null,null],
                                    [null,null,null]]);

const gameBoardFactory = (board) => {
    return {board};
}